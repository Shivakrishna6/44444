import tensorflow as tf\nimport json\n\n# Function to generate model metadata for Android\ndef generate_model_metadata(model, labels, input_specs, output_specs):\n    metadata = {\n        'model_name': model.name,\n        'labels': labels,\n        'input_specs': input_specs,\n        'output_specs': output_specs\n    }\n    return metadata\n\n# Example labels and specs\nlabels = ['label1', 'label2', 'label3']\ninput_specs = {\n    'input_shape': [256, 256, 3],\n    'data_type': 'float32'\n}\noutput_specs = {\n    'output_shape': [1, 3],\n    'data_type': 'float32'\n}\n\n# Load your model here\nmodel = tf.keras.models.load_model('your_model_path.h5')\n\n# Generate the metadata\nmetadata = generate_model_metadata(model, labels, input_specs, output_specs)\n\n# Save the metadata to a JSON file\nwith open('model_metadata.json', 'w') as json_file:\n    json.dump(metadata, json_file)\n\nprint('Model metadata generated successfully!')